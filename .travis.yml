sudo: required
env:
  global:
    - DOCKER_IMG=yglukhov/debian-stretch.base.wasm.nim
services:
  - docker
before_install:
  - docker pull $DOCKER_IMG
script:
  - docker run -v "$(pwd):/project" -w /project $DOCKER_IMG run "nimble install -y jsbind && nimble test && nimble testJsbind"
